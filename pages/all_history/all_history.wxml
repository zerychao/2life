<view class="container">
<!-- 获取头像昵称 -->
  <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
    <image class="userinfo-avatar" src="{{userInfo.avatarUrl}}" background-size="cover" bindtap="expandup"></image> 
    <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view>

   <!-- <view class="dairyContainer"> -->
       <!-- <scroll-view class="scrollview" scroll-y="true">
        <view wx:for="{{daryList}}" wx:for-item="item" wx:for-index="idx">
          <!-- <view class="dairyid"><text>{{idx}}</text></view> 
          <view class="dairytitle"><text>{{item.title}}</text></view>
        </view>
      </scroll-view> --> 
  <!-- </view>   -->
         <view class="testy" wx:for="{{dairyPartList}}" wx:for-item="itemy" wx:for-index="idy">
            <view class="testx" wx:for="{{itemy}}" wx:for-item="itemx" wx:for-index="idx">
                <text style="width:20px;height:20px;">{{itemx.content}}</text>
            </view>  
        </view>  

</view>